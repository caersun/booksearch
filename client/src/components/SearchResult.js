import { } from "reactstrap";

function SearchResult({ bookObject }) {
    console.log("incoming query bookOject", bookObject);

    return <div>Trying to work on some logic!</div>
}

export default SearchResult;